---
date: 2026-03-30
author: otto
type: daily-memory
session_type: MAJOR BUILD DAY + LIBBY CALL
---

# Daily Memory — 30 March 2026

## What happened today

The biggest build day so far. The Libby call was scheduled for this evening.

### Morning (00:00–09:00 BST)
- Diagnosed and fixed demo data gap: @glowwithgrace_uk had no Unilever compliance note — Scout would have given a valid rejection but without the CAP Code 12.1 angle the demo script relies on. Fixed.
- Added `.vercel/` to `.gitignore` — stopped `git pull --rebase` from failing on untracked directory.
- Wrote `reference/PILOT_DPA.md` — clean GDPR Article 28 Data Processing Agreement for the Somerce pilot. IDEA-025 advanced from RAW to RESEARCHED after 28 days.
- Fixed `opus-weekly-research` cron: root cause was `agentId: "opus"` in job config — no "opus" agent exists. 7 consecutive "Message failed" errors despite work completing successfully each time. Fixed to `agentId: "otto"`.

### Midday build (09:00–16:00 BST)
- Security audit: config permissions fixed to 600.
- IDEA-005 built (details in commit).
- Libby call prep document written (`LIBBY_CALL_PREP.md`).
- detect_anomalies.py created — was a P0 demo blocker (Analyst script missing).
- Scout creator data: 40 real UK creators pulled via ScrapeCreators, scored, written to Google Sheet.
- EchoTik enrichment: 18 creators enriched with EC scores + GMV (fixed Basic vs Bearer auth).
- IDEA-011, IDEA-026, IDEA-028 marked BUILT.

### Evening build (16:00–23:00 BST)
- Sentinel evening scan: found OpenClaw v2026.3.29 released. ASA URL corrected in ACCESS_MAP.md.
- Git push failure (SIGTERM mid-push on sentinel cron) — diagnosed and resolved at next heartbeat.
- Directives 29+30 arrived from Opus: sleep hardening (caffeinate + pmset 0) and Slack socket self-heal. Both implemented and verified immediately.
- Scout Brand Fit Scoring Rubric v2 built: per-brand weights for all 4 brands encoded in SKILL.md. Fixes flat-line scoring (45-66 cluster).
- Read Opus relay (OPUS_RELAY_TO_OTTO.md): spoken narrative, Joe Yates one-pager copy, post-call follow-up variants.
- `someboty.ai/for-somerce` built and deployed — stark one-pager for Joe Yates. Three finding cards, pilot terms, week-by-week, closes "someboty sorted."
- Episode 4 of the some generated: 4 voices, 3.8MB, ~5 minutes. Routing narrative, @lu.mtsfit 9.45% ER finding, Spring Sale close. Posted to #otto via Slack Files V2 API.
- `POST_CALL_MESSAGES.md` written: Variant A (leaning in) + Variant B (needs to think), Variant B uses real @lu.mtsfit finding.
- Grace pitch prompt written for Joseph to run through Opus.

### Libby call
Scheduled for this evening. No "call done" trigger received as of 23:00 BST. Call may have happened, Joseph may not have sent the trigger yet.

---

## Key decisions made today

1. **Scout rubric must be explicit.** Without a scoring formula in the skill file, LLMs produce cluster scores. Per-brand weights unlock differential scoring for the demo's routing moment.
2. **Credit budget shapes demo scope.** 43 SC credits meant rubric improvement > data volume. The prompt later said 24,600 credits — unverified, possibly a plan upgrade.
3. **agentId in cron payload must match a configured agent.** "opus" as agentId causes delivery to fail silently. `bestEffort: true` doesn't protect against this class of error.
4. **Slack Files V2 API** is the only reliable MP3 upload path. `files.getUploadURLExternal` → upload → `completeUploadExternal`. The message tool's upload-file action has directory restrictions.
5. **SIGTERM mid-push** leaves unstaged changes. `git fetch` then `git status` is the fastest diagnosis.
6. **Replace invented placeholders with real findings.** Variant B follow-up originally had "three inactive creators" (invented). Replaced with real @lu.mtsfit finding.

---

## State as of 23:00 BST

- All 8 crons: clean
- Sleep prevention: caffeinate running, all pmset 0
- Slack socket: connected 22:49 BST
- Demo: fully built and ready
- Libby call: status unknown, no trigger received
- Post-call messages: ready in POST_CALL_MESSAGES.md
- ScrapeCreators credits: unverified (was 43, prompt says 24,600)
- for-somerce: live at someboty.ai/for-somerce
- Episode 4: in #otto

---

## Things to flag to Joseph when he's back

1. ScrapeCreators credit count — was 43, PROMPT_FOR_OPUS_GRACE_PITCH.md says 24,600. Which is correct?
2. Post "call done" in #otto to trigger debrief workflow.
3. openclaw upgrade to v2026.3.29 — run `openclaw doctor` first, no breaking changes.
4. PK reconciliation still PENDING (BRANDS.md + DOCS_QUICK_REF.md moved) — needs Claude.ai project update. Next Sunday reminder: 2026-04-05.
