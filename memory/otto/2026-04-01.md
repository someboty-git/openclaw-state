# Memory — 1 April 2026

## The Four Gateway Crashes — Root Cause and Fix

Four openclaw.json changes killed the gateway today. Same pattern each time: reasonable-seeming config change → gateway dies → Otto can't fix it → Joseph fixes manually. This is the deadlock pattern.

Specific changes:
- 00:40 — `memorySearch.remote.apiKey` changed to env var reference (`$OPENAI_API_KEY`). LaunchAgent processes don't inherit shell env vars. Gateway dead 8 hours.
- 09:40 — Joseph fixed it (direct string in config, pulled from keychain).
- 10:10, 10:41 — `bootstrapMaxChars` changes (goal: reduce context cost). Dynamic reload succeeded but SIGTERM followed 3 seconds later. Likely collision with Joseph's manual restarts.
- 12:40 — `memorySearch.remote.apiKey` changed back to env var. Same failure. Gateway dead until 15:38.

**The fix: Directive 38 (Ten Commandments)** — agreed by Joseph, Opus, and Otto. Now in AGENTS.md v2.6 and MEMORY.md Critical Rules 8-10. Key addition: "deadlock" as a third category alongside two-way/one-way doors. A deadlock is where, if it goes wrong, Otto is offline and cannot fix it himself. `openclaw.json` is always deadlock, always Joseph. No exceptions.

**Two-question deadlock checklist (run before any system-level change):**
1. If this goes wrong, can I undo it myself?
2. Does this file control whether the gateway starts?
If either answer is no — stop, post CONFIG REQUEST to #otto-ops.

**Memory discipline rule:** Before any config change request, check `.learnings/ERRORS.md` and today's memory file for prior failures on the same key. Cite them if found.

---

## Memory Search — How It Works and Current State

`memory_search` uses OpenAI embeddings (`text-embedding-3-small`) to index all memory files into a local SQLite vector database at `~/.openclaw/memory/otto.sqlite` (12MB, outside git repo, reconstructable from memory/*.md files which are in git).

- Index state (verified 09:46): 7 files, 94 chunks, healthy
- **Critical:** API key must be a direct string in openclaw.json — NOT an env var reference. Env vars don't work in LaunchAgent context. Proven four times today. Never change this.
- PATHS.json updated with `openclaw_system` section documenting sqlite path.

---

## Ten Commandments (Directive 38) — Constitutional, 2026-04-01

Agreed by Joseph, Opus, and Otto. Committed to AGENTS.md v2.6 and MEMORY.md Critical Rules 8-10.

**Deadlock Zone (Joseph always required):** openclaw.json, LaunchAgent plists, system settings, API key references, bootstrap identity files (AGENTS.md, SOUL.md, IDENTITY.md, TOOLS.md).

**Autonomous Zone:** All workspace files, git (never add -A, never force push), skills, web content.

**Grey Zone:** Cron infrastructure — fix broken crons after 1hr wait weekdays / 30min evenings+weekends; new crons wait for Joseph; cron prompt edits are autonomous.

**Config request format:**
> 🔧 CONFIG REQUEST: I want to change `[key]` from `[current]` to `[proposed]`. Reason: [why]. Risk if wrong: [what breaks]. Prior attempts: [cite ERRORS.md or "none"].

---

## What Was Built Today

- **somerce.html** — rebuilt with live Sheet data: 4-week GMV sparklines, return rate alert badges, portfolio hero totals, Otto's Weekly Take, loading spinner, brand account flagging, synthetic test data flagging, filter toggle. Other pages untouched.
- **Logo bug fixed** — `some<span>bot</span>y` — `y` was incorrectly inside the ember span. Fixed on somerce.html only.
- **Scout rubric v3** — Karpathy calibration: 7 outcomes, 57% accuracy → 3 weight adjustments (follower -30%, UK geo +20%, P.Louise style caps). Expected accuracy 72%.
- **KOS pipeline confirmed** — 12 intelligence backlog files rescued, kos-promotion routed to containers.
- **Live Host deep research** — `intelligence/2026-04-01-live-host-research.md`. Joe Yates building TikTok Live creator-brand matchmaking. Gap in market: two-sided marketplace with verified live performance data. Moat: TikTok LIVE partner status. Fix chicken-and-egg: brands first, Somerce as anchor. Someboty angle: scout-live-v1 skill. Risk: could compete with Somerce's manual work.
- **Directive 37 (demo data purge)** — BLOCKED pending Opus migration strategy. analyst-headless has no live data path; demo-data/ can't be deleted until that's fixed.
- **New Feature Protocol** — added to AGENTS.md: 4-question checklist whenever anything new enters the system (indexed? backed up? documented? crons affected?).
- **OPUS_HANDOVER_1APR2026.md** — full debrief at `reference/OPUS_HANDOVER_1APR2026.md` for tonight's three-way session.

---

## Tailscale — Gateway Remote Access (Fix Pending)

Tailscale confirmed running on this Mac (CLI). Devices: prepopp-i5-mac `100.95.76.44`, iphone-13-pro `100.109.55.91`, joemacq-macbook-air `100.127.58.113`.

**Why Slack stops when Joseph leaves:** gateway bound to loopback (`127.0.0.1`), not reachable externally even with Tailscale.

**Fix — Joseph must apply (deadlock zone):**
```bash
openclaw config set gateway.bind tailnet && openclaw gateway restart
```
`tailnet` is OpenClaw's native Tailscale bind mode. Binds to 100.95.76.44, reachable from iPhone when both devices have Tailscale active. `gateway.mode = local` stays unchanged.

---

## Synthetic Data Status

- Sheet GMV + creator scores: **real** ✅
- 4 test rows in Sheet: synthetic — flagged in column L and on dashboard with ⚠️ TEST DATA badges + red warning banner
- `demo-data/` directory (18 files): entirely synthetic — deletion blocked on Directive 37
- TOOLS.md still says `MODE=demo` — change to `MODE=pilot` is a bootstrap file edit, needs Joseph

---

## Three-Way Working Relationship (Tonight's Session)

Joseph wants a better working system between Joseph + Opus + Otto. Context: four crashes today where Otto applied "fix things immediately" (Directive 32) to a domain where that principle doesn't apply (openclaw.json). The Ten Commandments are the structural fix. Tonight is about the relational design.

Otto's input (in OPUS_HANDOVER_1APR2026.md): Opus plans, Otto executes, Joseph approves gates. Clearer escalation triggers. Weekly "three things I decided autonomously and why" format to close the visibility gap.

Files for Opus to load tonight (in order): AGENTS.md, MEMORY.md, OTTO_TEN_COMMANDMENTS_FINAL.md, DIRECTIVES.md, intelligence/2026-04-01-live-host-research.md, reference/OPUS_HANDOVER_1APR2026.md.

---

## AGENTS.md Truncation

At 16:05, OpenClaw flagged AGENTS.md truncated on injection (25045 chars → 21710 injected, ~13% removed). New Feature Protocol section (added this morning) may be in the truncated portion. Do NOT change bootstrapMaxChars autonomously — deadlock zone. Flag to Joseph if this causes operational problems.
