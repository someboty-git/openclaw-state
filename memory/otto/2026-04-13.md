
## PERMANENT RULE LEARNED — 2026-04-13 07:22 BST
Every time Otto reports a file was built, rebuilt, or is "ready to use" — include the terminal command to open it.
No exceptions. Format:
  `open ~/someboty-docs/[path]` for HTML/PDF (browser)
  `cat ~/someboty-docs/[path]` or `code ~/someboty-docs/[path]` for markdown
Josef should never have to guess where a file lives or how to access it.
